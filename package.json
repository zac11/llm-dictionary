{
  "name": "llm-dictionary",
  "version": "1.0.0",
  "description": "Neuropaedia — The AI Encyclopaedia. Decode the language of LLMs, GenAI & AI Engineering (A–Z), in an interactive 3D library, with citations.",
  "type": "module",
  "scripts": {
    "terms": "node scripts/generate-terms.mjs",
    "stories": "node scripts/generate-stories.mjs",
    "og": "node scripts/generate-og.mjs",
    "graph": "node scripts/build-graph.mjs",
    "graph:check": "node scripts/build-graph.mjs --check",
    "graph:audit": "node scripts/audit-graph.mjs",
    "test": "node --test",
    "dev": "node scripts/build-graph.mjs && vite",
    "build": "node scripts/build-graph.mjs && node scripts/generate-og.mjs && vite build && node scripts/prerender-share.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "dependencies": {
    "three": "^0.169.0"
  }
}
