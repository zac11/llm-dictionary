{
  "name": "llm-dictionary",
  "version": "1.0.0",
  "description": "Tokenary — The AI Engineering Dictionary. Decode the language of LLMs, GenAI & AI Engineering (A–Z), in an interactive 3D library, with citations.",
  "type": "module",
  "scripts": {
    "terms": "node scripts/generate-terms.mjs",
    "og": "node scripts/generate-og.mjs",
    "dev": "vite",
    "build": "node scripts/generate-og.mjs && vite build && node scripts/prerender-share.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "dependencies": {
    "three": "^0.169.0"
  }
}
